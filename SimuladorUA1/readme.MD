### Instruções de uso

Para utilizar o simulador, deve ser gerado o arquivo .jar via "mvn clean package" ou compilação manual do projeto.

Após isso, basta rodar o arquivo passando o arquivo yaml de modelo como parâmetro:

``` 
java -jar fila-sim.jar <nomeDoArquivo.yaml>
```
